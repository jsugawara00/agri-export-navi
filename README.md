# 農産物輸出ナビ（Toika Export Navigator）

「調べる数ヶ月を、確かめる数分に。」

品目と国を選ぶだけで、農産物輸出の可否と難易度（輸出ハードル指数）、
減点内訳と対処案、必要な手続きのステップを数分で確認できるサイトです。
まずは山形県産の米・りんご・ラ・フランス × 台湾・香港・米国に対応しています。

**公開サイト: https://agri-export-navi.vercel.app**

## 運営の考え方

- **無料で、どなたでも使えます。** 輸出に興味のある方が自由に試し、
  現場の声で育ててもらうことを目指しています
- **会員登録・ログインはありません。** ユーザー管理を設けると使い勝手を
  落とすと考え、意図的にこの設計にしています。個人情報はお預かりしません
- その半面、**保存した案件はお使いの端末（ブラウザ）の中にだけ**残ります。
  PCとスマホの間でデータは共有されません。ブラウザの閲覧データを削除すると
  案件も消えます
- 検疫・制度の情報は誠実に保守しますが、**最終確認は必ず植物防疫所等の
  公的機関へ**お願いします。本サイトは判断の下調べを速くする道具です

## 使い方（3分）

1. トップで品目と国を選ぶ（例: りんご × 台湾）→ 地球儀が回って結果へ
2. 結果画面でハードル指数（100点からの減点方式・A〜E）と減点内訳、
   鮮度バッジ（情報の取得時期）を確認
3. 「この内容でナビゲートを始める」で案件として保存 → ステップナビへ
4. ナビでは、いま着手できるステップ・官庁確認ゲート・準備完成度が見えます。
   各ステップから書類ツール（英文契約書ひな形・インボイス/PL・港選定→
   乙仲リスト→相談メール下書き・銀行チェックリスト）が開けます
5. 保存後に輸出関連情報へ変更があった場合は、ナビ上部にお知らせが出ます

## 開発

```bash
npm install
npm run dev    # 開発サーバ
npm test       # 採点エンジン・content整合性・案件ロジックのテスト
npm run build  # 本番ビルド
npm run patrol # 区分B巡回（鮮度チェック＋情報源の差分検知→確認キュー起票）
```

- 判断基準（採点表・手順定義）は `content/` のmdが単一情報源。コードには埋め込まない
- スコアはLLMではなく決定論的な減点計算（`src/lib/score/engine.ts`）
- 実装方針の詳細は `AGENTS.md`・`docs/` を参照

## 情報鮮度の運用

- 週1回（月曜朝）、クラウドエージェントが情報源を巡回し、変更を検知すると
  **新旧差分と基準md修正案をPRとして起票**します（GitHubから運用者へ通知）
- **基準mdの自動書き換えは行いません。** 反映は運用者がPRを確認して
  マージしたときだけ（マージで自動デプロイ）。重大な変更（禁止品目・
  減点数値）は修正案を出さず必ず「要相談」扱いになります
- 手元での巡回は `npm run patrol`。運用コンソール `/ops` は環境変数
  `OPS_CONSOLE=1` の環境（運用者の手元）でのみ表示されます
- 鮮度上限（区分A/B/C）は `content/ops/patrol.md` が単一情報源

## クラウド保存を有効にする場合（現在は眠らせています）

Firebase未設定のままでローカル保存モードとして動作します（現行の公開設定）。
端末をまたいだ同期が必要になったら、`.env.local.example` を参考に
`NEXT_PUBLIC_FIREBASE_*` を設定してください（Firebaseコンソールで
Authentication の Googleプロバイダと Firestore を有効化。**Firestoreの
セキュリティルールで `projects` は `uid` 一致のみ読み書き可にすること**）。

---

※ 本サイトの検疫・制度情報は運用者が確認のうえ整備していますが、内容を
保証するものではありません。最終確認は植物防疫所等の公的機関へ。
